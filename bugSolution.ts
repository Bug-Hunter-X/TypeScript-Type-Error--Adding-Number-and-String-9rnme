function add(a: number, b: number): number {
  return a + b;
}

function addString(a: string, b: string): string {
  return a + b;
}

let result1 = add(5, 10); // Correct
let result2 = addString('5', '10'); // Correct for string addition